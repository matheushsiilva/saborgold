# Banco de dados local (SQLite) — desenvolvimento
DATABASE_URL="file:./dev.db"

# Supabase (PostgreSQL) — produção
# Obtenha em: Supabase Dashboard → Project Settings → Database → Connection string (URI)
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Senha do painel admin (opcional)
ADMIN_PASSWORD="saborgold2026"
